;; The arguments to the outer `prn` should be evaluated from left to
;; right.

;; Should print the args individually, then three nils, then return
;; nil.

;; 1
;; 2
;; 3
;; nil nil nil
;; => nil

(prn (prn 1) (prn 2) (prn 3))
